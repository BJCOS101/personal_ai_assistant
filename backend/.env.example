#template for project environment variables, listing required variables without including sensitive or secret information
OPENAI_API_KEY=your_open_api_key_here

USE_LOCAL_EMBEDDINGS=false

CHROMA_PERSIST_DIRECTORY=./data/chroma_db

DOCUMENTS_DIRECTORY=./data/documents

HOST=0.0.0.0
PORT=8000

CORS_ORIGINS=["http://localhost:3000"]